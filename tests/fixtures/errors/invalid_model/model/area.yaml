---
table:
  columns:
    state_name:
      nullable: false
      primary_keys: true
      foreign_key:
        - state.state_name
      type: VARCHAR(50)
    area_sq_mi:
      nullable: false
      type: INTEGER
process:
  materialization: table
  mode: drop
