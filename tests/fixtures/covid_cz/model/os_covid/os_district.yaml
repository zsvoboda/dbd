---
table:
  columns:
    county_id:
      foreign_keys:
        - os_county.county_id
      index: true
      nullable: false
    district_id:
      nullable: false
      primary_key: true
process:
  materialization: table
  mode: drop
